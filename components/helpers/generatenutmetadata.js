export default function generateNutMetadata(oldNut, publicQR_cid) {
  console.log("In Generating Nut Meta Data");
  let oldNutData = oldNut;

  let nutMetadata = publicQR_cid;

  return nutMetadata;
}
